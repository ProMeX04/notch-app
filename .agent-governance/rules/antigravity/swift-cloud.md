# Swift Architecture And Cloud Work

When working in `Sources/**`, `Tests/**`, or `Package.swift`, read
`docs/engineering/architecture-boundaries.md`.

For `Sources/Notch/Portal/**`, feature cloud synchronization, authenticated
clients, or durable outboxes, load
`.agents/skills/notch-swift-cloud-api-conventions/SKILL.md`.
